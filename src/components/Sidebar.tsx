import React from 'react';

const Sidebar: React.FC = () => {
  return (
    <div className="sidebar">
      <h1>Subject Sidebar</h1>
      {/* Add any filters for course, year level, or search functionality */}
    </div>
  );
};

export default Sidebar;
